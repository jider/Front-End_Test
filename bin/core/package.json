{
  "name": "core",
  "version": "0.0.1",
  "description": "Aplicación CORE del entorno de maquetación",
  "main": "core.js",
  "keywords": [
    "Documentación",
    "Repositorio",
    "CORE"
  ],
  "author": "Ignacio Díaz Rovina",
  "license": "MIT",
  "scripts": {
    "env": "env",
    "start": "node core.js",
    "postinstall": "bower --allow-root cache clean  && bower --allow-root install",
    "clean:dependencies": "rm -rf node_modules && rm -rf bower_components"
  },
  "dependencies": {
    "express": "^4.12.4",
    "express-handlebars": "^2.0.1"
  },
  "devDependencies": {
    "jshint": "^2.7.0"
  }
}
